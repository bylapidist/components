The Logo component is used for Lapidist branding.

```jsx harmony
import { ThemeProvider, Logo } from '@lapidist/components';

<ThemeProvider>
    <Logo styles={{ sizeWidth: 16 }} />
</ThemeProvider>
```
