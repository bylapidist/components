import { Meta, ArgsTable, Story, Source, Canvas } from '@storybook/blocks';
import * as LinkStories from './Link.stories.tsx';

<Meta of={LinkStories} />

# Link
> A primitive for hyperlinks. They should be used when you want to navigate to a new page, or to an anchor on the current
page. They should not be used to invoke actions (e.g. submitting a form or toggling an option) - use a 
[Button](?path=/docs/inputs-button--documentation) in these scenarios instead.

## Example
<Source dark />

## Controlling font size
The `variant` prop controls font size. There are three available font sizes, defined as `small`, `medium` and `large`.

### Small
> The `small` variant sets the font size to `--font-size__200`/`0.875rem`/`14px`
<Canvas>
    <Story of={LinkStories.Small} />
</Canvas>

### Medium
> The `medium` variant sets the font size to `--font-size__300`/`1rem`/`16px`
<Canvas>
    <Story of={LinkStories.Medium} />
</Canvas>

### Large
> The `large` variant sets the font size to `--font-size__400`/`1.125rem`/`18px`
<Canvas>
    <Story of={LinkStories.Large} />
</Canvas>

## Props
<ArgsTable />
