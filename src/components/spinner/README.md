The Spinner component is used for loading states.

```jsx harmony
import { ThemeProvider, Spinner } from '@lapidist/components';

<ThemeProvider>
    <Spinner styles={{ sizeWidth: 16 }} />
</ThemeProvider>
```
