import { Styles } from '@lapidist/styles';

export const textStyles = (): Styles => ({
    fontFamily: 'sans',
    fontWeight: 400,
    fontSize: '3',
    lineHeight: 'loose',
    textColor: {
        group: 'grey',
        shade: 'dark'
    }
});
