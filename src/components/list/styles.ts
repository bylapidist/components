import { Styles } from '@lapidist/styles';

export const orderedListStyles = (): Styles => ({
    listStyleType: 'decimal',
    listStylePosition: 'inside'
});

export const unorderedListStyles = (): Styles => ({
    listStyleType: 'disc',
    listStylePosition: 'inside'
});

export const listItemStyles = (): Styles => ({});
